# 实验操作记录

> 建立日期：2026-08-24

本文件从 2026-08-24 起只记录操作过程，包括：代码修改、Notebook/Kaggle/AutoDL 运行、上传与下载、命令、报错与修复、文件归档和实验决策。

数值配置、Val/Test 指标、逐类结果、证据等级和模型收益统一写入 `v6_overlap40_实验数据记录.md`。本文件不再复制结果表，以免同一数值在多个文档中漂移。

旧的 `实验现状总结.md` 保留为历史混合档案，不覆盖其中已有内容。

## 2026-08-24

- 建立独立的 v6 overlap40 数据台账与操作日志；从此将实验数据和操作过程分开维护。
- 只读核验 DSConv、Gated Boundary 与 DeepLab 的 config、metrics、完整 history 和训练曲线；确认模块目录已整理为 `results/v6_overlap40/M2/DSConv` 与 `results/v6_overlap40/M3/gated`。
- 检查训练与保存链路，确认现有模块产物未保存原始 5×5 confusion matrix；退化数值、错误方向和后续最小控制实验已写入数据台账，本操作日志不复制指标。
- 扩展统一评估脚本：支持在 `val` 上加载 DeepLab、DSConv、Gated checkpoint，保存原始/行归一/列归一 5×5 confusion matrix、前景二元矩阵、逐 Tile 错误、1/2/4/8 px 边界指标；Gated 额外保存 boundary-head 与 gate 激活统计。
- 模块训练入口增加纯 DeepLab 控制；新增 DeepLab physical batch=2/accum=2 与 Gated boundary_weight=0 两份独立配置。两份配置固定同一数据元文件 SHA-256，并继续禁止自动 Test。
- 更新 `notebooks/kaggle_v6_overlap40_modules.ipynb`，两个 Kaggle 账号可分别选择上述配置并行运行。
- 本地完成 11 项直接测试、三个旧 checkpoint 的单 batch Val 评估冒烟，以及 DeepLab/Gated 两条 64×64 单 epoch 完整产物链路短跑；全量 Val 与 80 epochs 正式运行留给本地长任务/Kaggle GPU。

## 2026-08-25

- 下载并归档 DeepLab batch2 控制、Gated boundary weight=0 batch2 控制和 Gated boundary weight=0 physical batch4 正式结果；逐项核验 config、metrics、80轮 history、checkpoint 和数据元文件 SHA-256。
- 确认 Gated batch4 产物目录为 `results/v6_overlap40/gated_boundary_resnet50_bw0_batch4/`，checkpoint 无 NaN/Inf，且未运行 Test；数值与模块判断写入独立数据台账。
- 新增可复用批量 Val 诊断脚本，支持统一混淆矩阵、边界指标、逐 Tile 统计和定性错误图，并将新 Gated batch4 checkpoint 加入实验注册表。
- 本地诊断首次遇到损坏 TIFF，定位并替换后增加 Val TIFF 完整性预检；随后发现 MX450 在 batch2 AMP 推理时产生非有限 logits，评价默认改为 FP32并增加 NaN/Inf 硬失败保护，错误的全背景结果作废。
- 根据 Gated batch4 的 Val 混淆矩阵确定模块2方向：前景类别互混极少，但前景 FP 比 FN 多51%，预测 Fault 的像素约30%来自背景；实现零初始化 Cross-Modal Consistency Residual（CMCR），用 WAC 与四通道地形证据产生5类残差 logits，初始输出严格等于当前 Gated。
- 新增 Gated+CMCR seed42 与 Gated seed1337 两个 physical batch4 并行配置；更新 Kaggle notebook，在正式训练前执行 batch4、512×512、AMP、AdamW完整单步显存冒烟，继续禁止自动 Test。
